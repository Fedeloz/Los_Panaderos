# Observation map design QA

final result: passed

## Reference and evidence
- Source: /Users/fedeloz/.codex/generated_images/01a0ba55-1ba7-76e2-9f08-d34e6cbd1f3d/exec-bdcfc639-a434-4beb-92c5-381dd351f36e.png (1536×1024).
- Implementation map: .runtime/population-ui/population-map-implemented.png (1600×1120 canvas, 800×560 logical coordinates).
- Desktop: .runtime/population-ui/population-desktop.png (1600×1300 viewport, device scale 1).
- Narrow screen: .runtime/population-ui/population-mobile.png (390×844 viewport, full-page capture, device scale 1).
- State: T+0, town 32 and farm 6 unwarned residents; both refuges empty.
- Source and implementation map were viewed together for comparison. Compared map content rather than the mockup's page header/footer. The implementation keeps the existing two-map layout and real crop; the generated reference's invented geographic proportions and distances are intentionally not copied.

## Findings and fixes
- P2, first capture: station icon overlapped the truck label. Moved the station annotation below vehicles; revised map capture shows clear separation.
- P2, narrow screen: existing timeline exceeded viewport width. Wrapped timeline controls; browser check now reports no horizontal overflow.
- No remaining actionable P0/P1/P2 findings within this desktop demo scope.

## Fidelity surfaces
- Typography: existing system sans retained; bold settlement headings, prominent group icon, count/status subtitle. Badges are legible at desktop map size.
- Layout: blue residential zone southwest, amber farm northeast, separate mint refuge markers, station annotation and external legend match the selected hierarchy. Actual geography, coordinates and scale retained.
- Colors: blue/gold population groups, mint refuges, dark green cards, cream text; reduced aerial dimming preserves field detail.
- Images and icons: original PNOA aerial imagery retained, Bootstrap Icons 1.13.1 MIT house/group/building paths bundled locally. Static hand-drawn zones and schematic homes communicate residences without implying surveyed household positions. No generated map is substituted for geography.
- Copy: actual group counts and states; refuge arrivals only for safe groups; blocked groups explicitly labeled blocked. Legend describes approximate zones and simulated residents.

## Verification
- All five population states exercised through the browser renderer: unwarned, evacuating, blocked, safe, burnt.
- Confirmed 32/6 population labels, 0 refuge arrivals before safety, and 6 farm arrivals after safety.
- Legacy frame without zone metadata renders without errors.
- Browser Runtime exception capture: none during verification.
- Narrow-screen overflow: false after fix.
- JavaScript syntax and git whitespace checks pass. Python suite passed 61 tests during this implementation.
- No real HappyRobot runs or phone calls triggered for visual QA.

## Follow-up polish
- Canvas labels shrink on phones; accessible population summary and existing people cards remain available. A zoomable mobile map is outside this desktop demo change.
- Schematic houses are visual orientation markers; the simulation continues to represent each settlement as one moving population group.

## Illustrated districts iteration — 19 September 2026

final result: passed

The user requested the generated illustration itself as the background and multiple population districts. Reference remains the approved image above; clean asset is `simulator/static/maps/brunete-illustrated.png` (1499×1049), generated by removing baked labels, markers and UI from that design. The source and final observation map were viewed together.

Evidence: `.runtime/district-ui/district-map-implemented.png` (1600×1120 backing canvas / 800×560 logical); `.runtime/district-ui/district-desktop.png` (1600×1300 viewport); `.runtime/district-ui/district-mobile.png` (390×844 full-page). Device scale 1. T+0, all four groups unwarned. Map-only comparisons exclude the reference's page chrome. Ground-truth and observation views both use the illustrated asset.

Intentional changes: three town cards instead of one; actual municipal census total distributed as explicitly estimated response districts; farm marked assumed occupancy; badges placed alongside the town to preserve its street visibility. The demo distance convention stays unchanged and is not represented as surveyed geography.

Fidelity surfaces: system typography, dark green cards, cream text, blue district fills, gold farm boundary and mint refuges follow the reference. The actual illustrated trees, houses and parcels replace the previous satellite image. No static people counts are baked into the image. Cards, arrivals and group positions are live overlays. Town districts sum to 11,261; farm is one separate assumed group of 6. Original sketch scale and invented district geometry are not claimed as official mapping.

Functional checks: 66 Python tests pass, including independent district warning/evacuation, town total conservation, a single farm district, warning target telemetry, district exposure counters and immutable recorded states. Browser rendering exercised unwarned, evacuating, blocked, safe and burnt statuses; aggregate town refuge arrivals reach 11,261 only when all three groups are safe; farm arrival count is 6. Legacy frames without zone metadata render. Browser runtime exceptions: none; narrow-screen horizontal overflow: false. JS syntax and whitespace checks pass. No external HappyRobot run or real phone call was made during verification.

No remaining actionable P0/P1/P2 findings. Existing P3: phone-sized canvas labels are small; the text population cards and accessible canvas summary provide the same state. Full mobile map zoom remains outside this demo task.

## Compact labels and observation contrast

final result: passed

User refinement: smaller labels outside the central map, dark background and lighter current sensor coverage. District cards reduced from 236×65 to 176×35 logical pixels and docked along the upper left/right margins. District names remain small inside their polygons. Current shared observations reveal the terrain through an otherwise dark overlay; stale/unseen areas stay dim. This uses each frame's observation timestamps, not hidden fire state. Town centre label offset prevents overlap with the station vehicle labels.

Evidence: `.runtime/compact-ui/compact-map-implemented.png`, `compact-desktop.png`, `compact-mobile.png`. Viewports and canvas density match prior checks. Browser checks cover all five population states and correct refuge arrivals; no runtime errors or mobile horizontal overflow. JS syntax/whitespace checks pass. No physics or workflow changes, so Python tests were not repeated for this visual refinement.

## Neighbourhood layout refinement

final result: passed

User requested housing/street-shaped neighbourhoods instead of North/Centre/South bands. Four irregular, nonoverlapping polygons now represent schematic Casco Histórico, Prado Alto, Prado Nuevo and Valle de los Rosales. Rosales uses the detached residential cluster; other boundaries trace illustrated street turns and housing edges. Four town groups sum to 11,261; farm remains one assumed group of 100. Actual geographical neighbourhood boundaries/positions are not claimed. Metadata map_label positions replace hardcoded label offsets.

Evidence: `.runtime/neighbourhood-ui/neighbourhood-map.png`, `neighbourhood-desktop.png`, `neighbourhood-mobile.png`; same viewport/density as previous iteration. First capture found Rosales' in-map text overlapping its docked card and the town-refuge caption crowding the fourth card. Labels inside the summary dock are now suppressed and the refuge caption is placed below its marker. Revised capture resolves both P2 findings. Compact typography, distinct pastel boundaries, dark unseen terrain and bright current observations remain unchanged; source/copy stays qualified as schematic allocation.

68 Python tests pass, including nonoverlapping polygon interiors and explicit Rosales evacuation. Browser verifies five population states, refuge arrival totals, no runtime errors and no horizontal overflow. JavaScript syntax and whitespace checks pass. No platform calls triggered for these checks. Retained integration IDs for the original groups; current districts payload exposes all five groups and explicitly overrides older enumerations.
