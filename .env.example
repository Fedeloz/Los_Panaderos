# Optional local-development value. The deterministic simulator requires no
# AI, database, phone, messaging or external map credentials.

# Allow the local Python adapter behind a named HTTPS tunnel.
PUBLIC_ORIGIN=
