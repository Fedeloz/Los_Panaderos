# Copy to .env. Only team members' own devices: these numbers/IDs receive real
# calls and Telegram messages from HappyRobot during the demo.

# HappyRobot target: dispatch (Despacho Central, calls + Telegram + drone) or drone (Los Panaderos only)
HAPPYROBOT_TARGET=dispatch

# Workflow and node ids. HappyRobot regenerates every node id when the workflow is
# edited, so keep them here instead of editing simulator/happyrobot.py. Copy each one
# from the node's panel in the workflow editor. Leave blank to use the code defaults.
HAPPYROBOT_WORKFLOW_ID=
HAPPYROBOT_DRONE_NODE=              # Ejecutar mision de dron
HAPPYROBOT_DISPATCH_NODE=           # Decision de Despacho
HAPPYROBOT_CALL_NODE=               # Llamar a esta persona
HAPPYROBOT_ZONE_ALERT_NODE=         # Enviar alerta de zona
HAPPYROBOT_PERSONAL_MESSAGE_NODE=   # Enviar informacion personal
# loop (default): frontend/sim never starts a Despacho run. Click Run on
# Despacho Central in HappyRobot development; the sim writes inbox/state to KV
# and the looping workflow pulls. push: legacy — each tick POSTs a new run.
HAPPYROBOT_MODE=loop
# Stable KV session key shared with Despacho Central (reset wipes this key).
DISPATCH_INCIDENT_ID=brunete-demo
# HAPPYROBOT_MCP_CONFIG=../.cursor/mcp.json
# HAPPYROBOT_MCP_SERVER=happyrobot-mcp-eu-all

# Shared incident state (Cloudflare Worker + KV in state-api/). Leave empty to disable publishing.
# Deploy: cd state-api && npx wrangler deploy && echo <token> | npx wrangler secret put STATE_API_TOKEN
STATE_API_URL=https://los-panaderos-state.<your-subdomain>.workers.dev
STATE_API_TOKEN=

# Demo personas (names/roles live in simulator/contacts.py).
# Same E.164 on farm + town + contact = one team handset; /lookup picks the
# more-endangered district when two people share the number.
DEMO_FARM_PHONE=            # Paco Herranz, Granja El Álamo (E.164)
DEMO_FARM_PERSON_CHAT_ID=   # his Telegram chat id
DEMO_TOWN_PHONE=            # Carmen Ortega, Prado Alto, Brunete
DEMO_TOWN_PERSON_CHAT_ID=

# Phone given to residents for more information (can match DEMO_*_PHONE)
DEMO_CONTACT_PHONE=

# District Telegram channels (alertar_zona). Empty → fallback 5916687836
# (Mensajes externos v2 demo destination). Env always wins when set.
DEMO_FARM_CHAT_ID=
DEMO_TOWN_CHAT_ID=          # Casco Histórico
DEMO_TOWN_NORTH_CHAT_ID=    # Prado Alto
DEMO_TOWN_SOUTH_CHAT_ID=    # Prado Nuevo
DEMO_TOWN_ROSALES_CHAT_ID=  # Valle de los Rosales
