# Copy to .env and fill in
DATABASE_URL=postgresql://wildfire:wildfire@localhost:5433/wildfire
API_TOKEN=dev-token-change-me

# External services
CESIUM_API_KEY=
NASA_KEY=
OSRM_BASE_URL=https://router.project-osrm.org

# Outbound-call allowlist (comma-separated E.164). Anything not listed here
# hard-fails in api/guards.py — never call a number that is not a team member's.
ALLOWED_PHONES=
